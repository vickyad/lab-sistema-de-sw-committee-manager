import { ITableHeader } from '../components/Table/TableHeader/type'

export const ComitteeTable: ITableHeader = {
  headers: ['órgão', 'vínculo', 'portaria', 'período', 'mandato'],
  sizes: [3, 1, 2, 3, 1],
  type: 'primary',
}
